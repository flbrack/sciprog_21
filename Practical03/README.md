# Practical 3

First we look at writing text.

Then we calculate the area under a curve using the trapezoidal rule.

We use a loop to accomplish this, for the function f(x) = tan(x), on the
interval [0, pi/3]. We then compare to the actual result, log(2).
